# Copy this file to .env.local and fill in your values.
# cp .env.example .env.local

# ── Databricks Auth ────────────────────────────────────────────────────────────
# Use either profile-based auth OR host+token (not both).

# Direct host + token (takes precedence over profile when both are set)
# DATABRICKS_HOST=https://my-workspace.cloud.databricks.com
# DATABRICKS_TOKEN=dapiXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# SQL warehouse ID for Unity Catalog queries (e.g. 8ba51d8cad2a3d9a)
DATABRICKS_WAREHOUSE_ID=

# ── Unity Catalog ─────────────────────────────────────────────────────────────
# catalog.schema where project tables and functions are created (e.g. my_catalog.my_schema)
PROJECT_UNITY_CATALOG_SCHEMA=

# ── Agent Model ───────────────────────────────────────────────────────────────
# Optional — if not set, derived at runtime as {DATABRICKS_HOST}/serving-endpoints/databricks-claude-sonnet-4-6/invocations
# Set only when the Foundation Model is on a different workspace (cross-workspace).
# AGENT_MODEL=https://other-ws.cloud.databricks.com/serving-endpoints/databricks-claude-sonnet-4-6/invocations

# PAT for the workspace hosting AGENT_MODEL — only needed for cross-workspace
# AGENT_MODEL_TOKEN=dapiXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# ── Databricks App ────────────────────────────────────────────────────────────
# App name for Databricks Apps deployment (e.g. my-agent-app)
DBX_APP_NAME=

# Local proxy target — leave as-is for local dev
API_PROXY=http://localhost:8000/invocations

# ── MLflow ────────────────────────────────────────────────────────────────────
MLFLOW_TRACKING_URI=databricks
MLFLOW_REGISTRY_URI=databricks-uc

# Set by create_mlflow_experiment.py or create_all_assets.py (e.g. 3688767017703470)
MLFLOW_EXPERIMENT_ID=

# ── Genie ─────────────────────────────────────────────────────────────────────
# Set by create_genie_space.py or create_all_assets.py (e.g. 01f0000000000000000000000000000a)
PROJECT_GENIE_CHECKIN=

# ── Env Store ─────────────────────────────────────────────────────────────────
# The store workspace is independent from the project workspace (can be a shared catalog).
# Set via ./scripts/init_env_store.sh
#
# Workspace hosting the store volume (e.g. https://my-shared-workspace.cloud.databricks.com)
ENV_STORE_HOST=
# PAT for the store workspace — use token OR profile, not both
ENV_STORE_TOKEN=
# Base UC Volume path for saving/loading .env.local snapshots (e.g. /Volumes/my_catalog/my_schema/store)
ENV_STORE_CATALOG_VOLUME_PATH=

# ── Knowledge Assistants ──────────────────────────────────────────────────────
# Endpoint name for each KA — set after deploying via forge-add-ka (e.g. ka-xxxxxxxx-endpoint)
PROJECT_KA_PASSENGERS=
